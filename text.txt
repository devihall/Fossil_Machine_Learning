<script src="https://unpkg.com/ml5@latest/dist/ml5.min.js"></script>

// document.getElementById("file-input1").addEventListener("change", function () {
    //   console.log("hey")
    //   var categoryText = document.querySelector("#category").textContent;
    //   console.log("Category:", categoryText);
    // });
    //   console.log("Category:", categoryText);

    // Use the h4TextContent as the category
    // let category = h4TextContent.trim();

    // let category = document.getElementsByClassName("category")[0].innerText;
    // console.log("category", category);

    //     let category = document.getElementsByClassName("category").innerText;

    // let element = document.getElementById("category"); // Replace "example" with the ID of your element
    // let category = document.getElementById("category").textContent;

    // var element = document.getElementsByClassName("category"); // Replace "example" with the ID of your element
    // console.log(" element", element);
    // var category = element.textContent;
    // console.log("category", category);

    // var element = document.getElementById("example"); // Replace "example" with the ID of your element
    // var idValue = element.getAttribute("id");

    // var element = document.getElementById("category"); // Replace "example" with the ID of your element
    // var category = element.innerHTML;
    // console.log("category", category);

    // var element = document.querySelector(".category"); // Replace ".example" with the class name of your element
    // var category = element.textContent;
